version: "3.8"

services:
  blockchain:
    image: trufflesuite/ganache
    command: --chain.chainId=31337
    ports:
      - "8545:8545"

  deployer:
    build:
      context: .
      dockerfile: docker/Dockerfile
    depends_on:
      - blockchain
